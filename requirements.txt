# Core deep learning stack
# For CUDA 11.8 on Linux training servers, install PyTorch with:
# pip install torch==2.1.0 torchvision==0.16.0 torchaudio==2.1.0 --index-url https://download.pytorch.org/whl/cu118
# For macOS/local development, the same pinned versions from the default index are typically sufficient.
torch==2.1.0
torchvision==0.16.0
torchaudio==2.1.0

# Modeling and training
transformers==4.36.2
torch-geometric==2.4.0
einops==0.7.0
omegaconf==2.3.0
wandb==0.16.2

# Data and evaluation
numpy==1.26.3
pandas==2.1.4
scikit-learn==1.3.2
tqdm==4.66.1
scipy==1.11.4

# Video and preprocessing
opencv-python==4.9.0.80
# decord wheels are often unavailable via pip on macOS, especially on Apple Silicon.
# Install it separately on Linux/CUDA servers or via conda-forge when available.
decord==0.6.0; platform_system != "Darwin"
ffmpeg-python==0.2.0
librosa==0.10.1

# Face detection and tracking
# facenet-pytorch 2.6.0 requires torch>=2.2.0, so keep this aligned with torch 2.1.0.
facenet-pytorch==2.5.3
retina-face==0.0.17
deep-sort-realtime==1.3.2
